# Tremor 3.x peer-pins react@^18. React 19 works at runtime for the parts we
# use (charts render; no new React 19 features are leaned on), but npm's
# strict peer resolver would otherwise fail the install.
legacy-peer-deps=true
